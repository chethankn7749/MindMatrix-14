<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".utils.HastaKalaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HastaKalaShop">

        <activity
            android:name=".activities.IncomeLogActivity"
            android:exported="false"
            android:theme="@style/Theme.HastaKalaShop" />
        <activity
            android:name=".authentication.SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.HastaKalaShop" />
        <activity
            android:name=".authentication.SignInActivity"
            android:exported="false"
            android:theme="@style/Theme.HastaKalaShop" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.HastaKalaShop" />
        <activity
            android:name=".authentication.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.HastaKalaShop.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
